# Copy response

Let a user copy generated text or another output value.

```html
<pixel-ui-element variant="copy" text="Generated output"></pixel-ui-element>
```

Import `pixel.md/elements`; provide the current response with `text` and connect the copy action to app feedback if needed.
