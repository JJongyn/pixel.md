# Background toggle

Expose a compact on/off control for an optional agent behavior.

```html
<pixel-ui-element variant="toggle" label="Background actions"></pixel-ui-element>
```

Import `pixel.md/elements`; listen for `pixel-element-change` and persist the chosen setting in app state.
