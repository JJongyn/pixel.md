# Response stream

Show text that is arriving progressively from a real response stream.

```html
<pixel-ui-element variant="stream" text="The response so far…"></pixel-ui-element>
```

Import `pixel.md/elements`; update `text` from the actual stream and stop it when generation completes.
