# Anchor chat effect

Use corner accents that gently breathe around the composer frame.

```html
<pixel-chat-effect variant="anchor"><your-existing-composer></your-existing-composer></pixel-chat-effect>
```

Import `pixel.md/chat-effect`. This effect decorates the frame; it does not provide the composer UI.
