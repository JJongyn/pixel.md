# Tilt chat effect

Use when the pixel frame should rotate around a horizontal axis.

```html
<pixel-chat-effect variant="tilt"><your-existing-composer></your-existing-composer></pixel-chat-effect>
```

Import `pixel.md/chat-effect`. Keep the existing composer fixed in its layout.
