# Verifying indicator

Use while checking the result for correctness or completeness.

```html
<pixel-agent-status state="verifying" label="Verifying result" detail="Final check"></pixel-agent-status>
```

Import `pixel.md/indicator`. Show this only when a real verification step is in progress.
