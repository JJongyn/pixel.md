# Reading indicator

Use when the agent is consuming a document, page, or user-provided context.

```html
<pixel-agent-status state="reading" label="Reading sources" detail="4 sources"></pixel-agent-status>
```

Import `pixel.md/indicator`. The detail can name the active document or source count.
