# Planning indicator

Use while choosing or ordering the steps that will address the request.

```html
<pixel-agent-status state="planning" label="Planning next steps" detail="3 actions"></pixel-agent-status>
```

Import `pixel.md/indicator`. Move to `acting` when execution starts.
