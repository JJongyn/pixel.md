# Thinking indicator

Show this while the agent is reasoning or choosing its next step. Update it to the next real state when work changes.

```html
<pixel-agent-status state="thinking" label="Thinking…" detail="Finding an approach"></pixel-agent-status>
```

Import `pixel.md/indicator`. `detail` is optional; keep the label concise.
