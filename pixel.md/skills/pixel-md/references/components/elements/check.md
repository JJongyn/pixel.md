# Approval check

Capture a small confirmation or completion selection.

```html
<pixel-ui-element variant="check" label="Mark as reviewed"></pixel-ui-element>
```

Import `pixel.md/elements`; use the emitted event to update the corresponding application state.
