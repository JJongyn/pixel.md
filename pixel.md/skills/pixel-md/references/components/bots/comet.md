# Nari bot avatar

Use Nari for a quick-response helper or a short-running task agent.

```html
<pixel-bot-avatar variant="comet" state="success" label="Nari" size="medium"></pixel-bot-avatar>
```

Import `pixel.md/bot`. The success animation is a visual cue; update it only after the app confirms completion.
