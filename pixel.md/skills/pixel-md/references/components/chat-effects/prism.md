# Prism chat effect

Use for a restrained diagonal 3D movement around a composer.

```html
<pixel-chat-effect variant="prism"><your-existing-composer></your-existing-composer></pixel-chat-effect>
```

Import `pixel.md/chat-effect`. Keep the diagonal motion within the frame so adjacent controls remain clear.
