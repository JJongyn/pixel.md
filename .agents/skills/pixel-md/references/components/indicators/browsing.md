# Browsing indicator

Use while navigating web pages or other browsable resources.

```html
<pixel-agent-status state="browsing" label="Browsing the web" detail="3 pages"></pixel-agent-status>
```

Import `pixel.md/indicator`. Use `searching` for finding sources and `browsing` for moving through them.
