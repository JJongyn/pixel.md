# Plan selection

Present alternative next steps or plans for the user to choose.

```html
<pixel-ui-element variant="branch" items='["Quick fix","Full review"]'></pixel-ui-element>
```

Import `pixel.md/elements`; handle selection from `pixel-element-change` and continue with the chosen app workflow.
