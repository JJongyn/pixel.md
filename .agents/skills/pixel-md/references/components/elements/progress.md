# Workspace progress

Show progress for a real task such as indexing or file processing.

```html
<pixel-ui-element variant="progress" label="Indexing workspace" value="62"></pixel-ui-element>
```

Import `pixel.md/elements`; keep `value` synchronized with the task's actual progress.
