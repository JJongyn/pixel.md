# Task queue

Summarize queued or upcoming tasks in an agent workflow.

```html
<pixel-ui-element variant="queue" items='["Prepare report","Review sources"]'></pixel-ui-element>
```

Import `pixel.md/elements`; update `items` as the real task queue changes.
