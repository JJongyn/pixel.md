# Send approval

Ask for confirmation before an action that requires user approval.

```html
<pixel-ui-element variant="approval" label="Approve sending"></pixel-ui-element>
```

Import `pixel.md/elements`; the component only emits intent. Run the approved operation in the app after handling the event.
