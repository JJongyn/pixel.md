# Retry request

Offer another attempt after a recoverable failure.

```html
<pixel-ui-element variant="retry" label="Try again"></pixel-ui-element>
```

Import `pixel.md/elements`; handle `pixel-element-change` by triggering the application's retry behavior.
