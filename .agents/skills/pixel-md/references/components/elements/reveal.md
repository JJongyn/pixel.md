# Activity reveal

Let users expand or collapse secondary activity details.

```html
<pixel-ui-element variant="reveal" label="Show activity"></pixel-ui-element>
```

Import `pixel.md/elements`; keep the revealed content and expanded state connected to the host UI.
