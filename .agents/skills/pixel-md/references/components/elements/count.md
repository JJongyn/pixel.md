# Count up

Show a changing numeric result such as completed tasks.

```html
<pixel-ui-element variant="count" label="Tasks completed" value="128"></pixel-ui-element>
```

Import `pixel.md/elements`; update `value` from the real count in application state.
