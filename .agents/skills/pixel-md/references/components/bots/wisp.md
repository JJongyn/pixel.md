# Bomi bot avatar

Use Bomi for a quiet background worker or a low-priority agent.

```html
<pixel-bot-avatar variant="wisp" state="sleeping" label="Bomi" size="small"></pixel-bot-avatar>
```

Import `pixel.md/bot`. Set `sleeping` only when the agent is genuinely idle or paused.
