# Taeo bot avatar

Use Taeo for tool-oriented agents that execute operations or connect services.

```html
<pixel-bot-avatar variant="gear" state="working" label="Taeo" size="medium"></pixel-bot-avatar>
```

Import `pixel.md/bot`. Pair it with a status label when users need to know which tool is running.
