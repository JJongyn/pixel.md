# San bot avatar

Use San as a friendly default identity for a single assistant or the current speaker.

```html
<pixel-bot-avatar variant="mote" state="thinking" label="San" size="medium"></pixel-bot-avatar>
```

Import `pixel.md/bot`. Update `state` from the agent's real activity.
