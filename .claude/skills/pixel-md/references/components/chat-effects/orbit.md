# Orbit chat effect

Add low-key pixel signals moving along the chat input perimeter.

```html
<pixel-chat-effect variant="orbit"><your-existing-composer></your-existing-composer></pixel-chat-effect>
```

Import `pixel.md/chat-effect`. Keep the input's existing keyboard, focus, and send behavior intact.
