# Bevel chat effect

Use when the pixel frame should rotate around a vertical axis with a compact 3D turn.

```html
<pixel-chat-effect variant="bevel"><your-existing-composer></your-existing-composer></pixel-chat-effect>
```

Import `pixel.md/chat-effect`. The frame rotates around the existing composer; do not add separate orbiting objects.
