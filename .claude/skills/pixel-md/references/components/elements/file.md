# Attachment chip

Represent a file attached to a prompt or returned by an agent.

```html
<pixel-ui-element variant="file" label="brief.pdf"></pixel-ui-element>
```

Import `pixel.md/elements`; use a meaningful filename and connect selection to the app's file preview or download.
