# Using tool indicator

Use while an external tool is running or returning data.

```html
<pixel-agent-status state="tool" label="Using tool" detail="Waiting for results"></pixel-agent-status>
```

Import `pixel.md/indicator`. Use a clear label if the user benefits from knowing which tool is active.
