# Searching indicator

Use while looking through indexed knowledge, files, or a set of sources.

```html
<pixel-agent-status state="searching" label="Searching docs" detail="4 sources"></pixel-agent-status>
```

Import `pixel.md/indicator`. Update the label or detail when the searched scope changes.
