# Saved toast

Confirm that a lightweight action has completed.

```html
<pixel-ui-element variant="toast" label="Saved"></pixel-ui-element>
```

Import `pixel.md/elements`; show it in response to a successful app action, not as proof of a request that has not completed.
