# Workflow stepper

Summarize ordered stages in a multi-step agent task.

```html
<pixel-ui-element variant="steps" items='["Research","Draft","Review"]'></pixel-ui-element>
```

Import `pixel.md/elements`; keep the active and completed steps aligned with the actual workflow.
