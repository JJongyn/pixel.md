# Usage meter

Show a quota, budget, or other bounded usage value.

```html
<pixel-ui-element variant="usage" label="Monthly credits" value="68"></pixel-ui-element>
```

Import `pixel.md/elements`; provide a current value from the product's own usage data.
