# Tool handoff

Explain that work is moving from the agent to a tool or another worker.

```html
<pixel-ui-element variant="handoff" label="Handing off to browser"></pixel-ui-element>
```

Import `pixel.md/elements`; emit the handoff UI alongside the real app operation and update it when the result returns.
