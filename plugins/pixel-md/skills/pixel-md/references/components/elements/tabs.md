# Workflow tabs

Let users switch between a small set of related views.

```html
<pixel-ui-element variant="tabs" items='["Overview","Activity","Files"]'></pixel-ui-element>
```

Import `pixel.md/elements`; handle `pixel-element-change` to change the application's selected view.
