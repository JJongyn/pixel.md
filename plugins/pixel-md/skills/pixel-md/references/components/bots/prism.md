# Haru bot avatar

Use Haru for an analysis, evaluation, or review-focused agent.

```html
<pixel-bot-avatar variant="prism" state="thinking" label="Haru" size="medium"></pixel-bot-avatar>
```

Import `pixel.md/bot`. Change the state attribute as the agent begins and completes its review.
