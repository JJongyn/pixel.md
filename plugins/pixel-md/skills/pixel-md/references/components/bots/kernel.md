# Mino bot avatar

Use Mino as a central coordinator or system-level assistant identity.

```html
<pixel-bot-avatar variant="kernel" state="idle" label="Mino" size="large"></pixel-bot-avatar>
```

Import `pixel.md/bot`. `label` is exposed to assistive technology together with the current state.
