# Hop chat effect

Use a small, shallow pixel bounce around the composer when a touch of liveliness is useful.

```html
<pixel-chat-effect variant="hop"><your-existing-composer></your-existing-composer></pixel-chat-effect>
```

Import `pixel.md/chat-effect`. Keep the motion subtle in dense chat layouts.
