# Building indicator

Use while composing or assembling an answer, artifact, or other output.

```html
<pixel-agent-status state="building" label="Building answer" detail="Drafting"></pixel-agent-status>
```

Import `pixel.md/indicator`. Transition to `verifying` if the result is checked before delivery.
